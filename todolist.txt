Add Timestamp to config page for user reference

Fade out 'delivery configured successfully message' {{feedback}} and update timestamp on save
dialog box directive?

add prefix options to sequential filenames

prevent titles from rendering links to preview urls if none specified

prevent download column display for parent list items

Skins

	Add dark display colors

nested list:

	add breadcrumbs (or back links) to public display
	Prevent nesting more than 1? level deep?
	
multiple projects functionality:
	
	New main config page
		Create a new project
		Edit a project
		
Stylesheets
	So much...
	